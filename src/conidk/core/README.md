# Core Module

The `core` module contains the fundamental building blocks and base classes for the library.

## Contents

- **Authentication**: Handles Google Cloud authentication.
- **Configuration**: Global configuration settings.
- **Base Classes**: Base classes for other components in the library.

These components are designed to be used by higher-level modules (Wrappers and Workflows) but can also be used directly for custom implementations.
